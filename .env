# Database Configuration
# Option 1: Local PostgreSQL (requires PostgreSQL installation)
# DATABASE_URL="postgresql://bayg_user:bayg123@localhost:5432/ratecard_bayg"

# Option 2: Neon serverless PostgreSQL (recommended - sign up at neon.tech)
# DATABASE_URL="postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require"

# Option 3: Using localhost PostgreSQL (configured for trust authentication)
DATABASE_URL="postgresql://postgres@localhost:5432/ratecard_bayg"

# Development Configuration
NODE_ENV=development
PORT=5000

# Session Secret (change this to a random string in production)
SESSION_SECRET="your-super-secret-session-key-change-in-production"

# SMTP Configuration (Optional - for email functionality)
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
